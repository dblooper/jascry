import {factory} from "../../ConfigLog4j"
import { PostDto } from "../dto/PostDto";

const LOGGER = factory.getLogger("service.Post");

export class PostService {

    saveNewPost(postDto: PostDto) {

    }
}